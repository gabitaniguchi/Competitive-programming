#include <bits/stdc++.h>
#include <string.h>

using namespace std;

int main()
{
    char s[50];
    int x;
    scanf("%s", s);
    
    x = strlen(s);
    
    printf("%d", x);
    
    return 0;
}